# Development log

## 2026-07-16

- Used AI assistance to translate the discovery findings and assignment constraints into a bounded deterministic retrieval design.
- Chose a compact Python package with `pypdf`, standard-library CLI/JSON support, and `unittest`; no service, database, model call, or external API was introduced.
- Defined fixed ticker-evidence weights and confidence margins so header/title evidence dominates incidental body mentions and tied/weak cases cannot be selected silently.
- Added punctuation/spacing normalization for Bloomberg inputs and a `GY`/`GR` alias based on the discrepancy already documented in discovery.
- Added synthetic unit tests and a manually verified filename-only local evaluation set. No report passages, page images, or PDF-derived fixtures were added to the repository.
- The first corpus evaluation exposed a split table-header/value layout. Updated deterministic classification to recognize Bloomberg/ticker labels on either of the two preceding extracted lines and added a regression test without lowering the safety threshold.
- Kept summarization, claim citation generation, charting, and email drafting/sending out of this vertical slice.
- Initial slice verification passed 12 tests, including all 11 manually verified corpus cases. A real not-found CLI run inspected only the first two pages of the seven filename-shortlisted reports and returned a transparent JSON result.
- Performed a skeptical senior-engineer self-review. Material findings included unsafe selection around unreadable candidates, report-line leakage in evidence output, over-permissive date/ticker parsing, broad field/header heuristics, Windows-formatted result paths, and missing ignore rules for the actual local brief filename and Python build artifacts.
- Hardened selection, privacy, normalization, path serialization, symlink handling, and error sanitization without adding dependencies or expanding beyond retrieval.
- Removed raw evidence snippets and the initially considered line hash from serialized output; retrieval evidence now contains locator/classification metadata only.
- Added real synthetic invalid-payload and encrypted-PDF tests so parser failure handling is verified rather than assumed.
- Post-review verification passed all 21 tests, including the 11-case local corpus evaluation and the new adversarial error, privacy, portability, and false-positive tests.
- Retrieval milestone acceptance run: reran the complete suite with 21/21 passing and 11/11 manually verified corpus queries selecting the expected local filename.
- Audited commit candidates for PDFs, PDF payloads, recipient watermarks, analyst contact data, extracted report passages, absolute workstation paths, and serialized raw evidence. No report PDF or proprietary extracted text is included in the milestone artifacts.
- Added `docs/evaluation.md` with filename-only results, safety behavior, and known limitations. No later milestone work was started.
- Inspected the retrieval implementation before starting the next slice and kept deterministic report selection unchanged.
- Added a PyMuPDF evidence adapter for only the uniquely selected report. It preserves page boundaries and emits stable block identifiers, exact extracted text, bounding boxes, page dimensions, and source block numbers as structured JSON without writing derivative artifacts.
- Prototyped exact-reference resolution and tamper checks, then replaced that flat-block prototype with the requested nested page/block/word schema before the milestone review.
- Added synthetic page-boundary, ID-stability, JSON, malformed-input, and page-range tests plus runtime-only corpus tests. No report passages or coordinates were added to repository artifacts.
- Kept estimate extraction, claim selection, highlighting/viewer generation, and LLM summarization out of this slice.
- Installed the declared PyMuPDF dependency locally and ran the full suite: 25 tests passed, 0 failed, 0 skipped. Verified every generated evidence block resolved back to its original page in all four tested broker reports.
- Expanded the evidence slice to the requested nested document/page/block/word schema and added content-derived document IDs, one-based page-range filtering, a dedicated `find-rpt evidence` subcommand, direct local-path extraction, and explicit unreadable/encrypted/textless/page-range errors. The legacy positional retrieval command remains compatible.
- Real-layout validation now covers five brokers/layouts: ABG, BofA, J.P. Morgan, Nordea, and Kepler. No report text, coordinates, screenshots, or derived page artifacts were persisted.
- The first five-layout run exposed a Nordea text box slightly beyond the page boundary. Added deterministic page clipping and degenerate-box omission, then reran the evidence tests successfully (6/6).
- Manual verification included parsing evidence JSON from the direct-path CLI, checking one-based page filtering, confirming absolute paths are absent from serialized output, and comparing every returned real-report block against text extracted from its own source page in memory.
- Final verification passed the complete suite: 28 tests, 0 failures, 0 skips. This includes an in-memory locator CLI run that selected the Kepler report and extracted only page 1, without writing evidence output to disk.
- Pre-commit review reran all 28 tests successfully and exercised the actual locator-based evidence CLI twice across ABG, BofA, J.P. Morgan, Nordea, and Kepler. All 50 pages had correct one-based sequences on both runs, every emitted block rectangle remained within its page, and repeated block ID lists were identical.
- Manually checked block ordering against PyMuPDF's sorted extraction sequence and spot-checked header/body progression across the five layouts. Dense multi-column table semantics remain explicitly out of scope.
- Audited tracked and untracked candidate changes for PDFs, source writes, proprietary passages, cached text, absolute paths, large files, unnecessary dependencies, and broker-specific branches. No material implementation issue was found; corrected stale documentation describing the superseded flat-block resolver prototype.
- Read the complete retrieval/evidence implementation, repository requirements and decisions, development history, and all existing tests before beginning the revision-candidate slice. The baseline suite passed 28/28 tests.
- Added a deterministic revision schema with three result states, stable block evidence references, explicit confidence/method/warning fields, period-basis preservation, and separate relative-percentage and percentage-point fields. Missing data remains null.
- Implemented candidate-page/block detection plus parsers for prose old/new statements, aligned old/new/consensus tables, grouped multi-year new/old/change tables, percentage-only matrices, and compact inline change headers. No report selection, arithmetic, value joining, or evidence coordinate is delegated to a model.
- Added deterministic normalization for common financial metrics and accounting qualifiers, currencies, thousands/millions/billions, per-share values, percentages, percentage points, basis points, and fiscal/calendar period forms. Negative values, zero denominators, non-meaningful states, unit mismatches, source conflicts, and display-rounding mismatches are explicit.
- Chose `(new-old)/abs(old)` for relative revisions and consensus spreads, with zero-denominator warnings and a 0.25 percentage-point stated/calculated reconciliation tolerance. The extractor never back-solves an old value from a stated percentage.
- Integrated `find-rpt revisions` for both locator-backed selection and a single explicit local PDF. Locator failures preserve existing exit/status behavior; revision output is JSON only and contains values plus safe evidence references, not source passages.
- Real-layout review exposed and corrected false joins between side-by-side summary/revision tables, misclassification of current-forecast/change tables as old/new pairs, lost period columns in grouped tables, metric-acronym/currency confusion, and source statements with conflicting target values.
- Added synthetic arithmetic, normalization, prose, table, consensus, no-revision, unresolved, conflict, repeatability, CLI, and evidence-resolution tests plus an 11-report metadata-only regression set. No proprietary text, values, coordinates, screenshots, or analyst data were added to fixtures.
- The initial revision evaluation emitted 178 source-traceable rows across six reports, reconciled 79 of 98 comparable stated/calculated changes, warned on 19 display-rounding mismatches, and retained two conflicting source candidates. The subsequent pre-commit review refined the manual accuracy accounting below.
- No LLM or external dependency was introduced. Temporary rendered inspection pages stayed in the operating-system temp directory and were not added to the repository.
- Pre-commit review ran the public locator-backed `revisions` CLI against all 11 reports and manually inspected representative pages from every revision-bearing layout plus no-revision and unresolved cases. It included explicit tables, prose, consensus, negative values, percentage-point rows, and ambiguous extraction.
- Fixed three material review findings with generic deterministic rules: added exact same-page consensus enrichment with additional evidence, expanded abbreviated accounting qualifiers, and excluded disclosure/legal boilerplate from candidate scoring. No broker-specific branch was added.
- Manually field-checked 32 emitted rows across the six revision-bearing reports: 32 were correct, no false-positive row was found, and 45 explicit dense-table rows were counted as known conservative misses. Automated checks resolved all 493 block references for all 178 emitted rows. Arithmetic reconciled for 79 of 98 comparable rows (80.6%); the 19 retained mismatches are explicitly warned.
- Final review suite passed 41/41 tests. Two reports return `no_revisions`, three retain unresolved candidates without fabricated rows, and six emit revisions. Temporary page renders remained local to the operating-system temp directory and were removed after inspection.
- Read the full repository instruction set, requirements/discovery/decision/evaluation documentation, development history, retrieval/evidence/revision implementations, and all tests before starting the bounded-rationale milestone. The unchanged baseline passed 41/41 tests.
- Added a deterministic candidate-passage selector anchored to revision evidence, same-page adjacency, nearby-page metric/signals, opening context, and explicit page cross-references. Disclosure noise is excluded and model input is capped at 24 blocks and 12,000 characters.
- Added the `RationaleModel` protocol, a deterministic fake provider, and a configured OpenAI-compatible provider restricted to loopback endpoints. API key, URL, and model name are environment-only. No external endpoint, browsing, whole-PDF transfer, report cache, or prompt log was added.
- Added structured output for rationale clarity, causal drivers, impacted metrics/periods, categories, why-now, report context, management contact, people met, takeaway, jargon, first-read items, confidence, evidence block IDs, and warnings.
- Added deterministic post-model validation for schema shape, selected-document and selected-passage block identity, lexical and numeric claim support, metrics, fiscal periods, causal-link strength, context, management interaction, names, roles, jargon, and model failures. Unsupported claims are removed, explicit causal links can be downgraded, and unexplained revisions become `unclear`.
- Added `find-rpt rationale` for locator-backed or direct-path extraction plus `--no-model` passage-retrieval mode. Missing model configuration fails clearly. Model failures return explicit warnings and no invented fallback interpretation.
- Added synthetic non-proprietary tests covering all requested rationale states and failure modes plus a twelve-report local retrieval-only evaluation, including the discovered Goldman Sachs management-meeting layout. No proprietary passage, prompt, person, coordinate, or value was committed.
- The first real CLI sweep exposed Windows CP1252 failures on report symbols. Reconfigured supported standard streams to UTF-8 and reran the complete twelve-report sample successfully.
- No local rationale model endpoint or key was configured. Because repository policy prohibits external report transfer, real-report semantic accuracy and manual claim review were not fabricated; they remain an explicit local-model release gate. The retrieval-only sample averaged 23.8 blocks and 7,897.7 passage characters, with maxima of 24 and 11,987. Including the fixed system prompt and JSON metadata, estimated total input ranged from 7,174 to 16,531 characters and averaged 12,495.9.
- Final rationale implementation suite passed 53/53 tests. The bounded input was repeatable for all twelve real reports, every selected block ID resolved to its source report, and no input exceeded configured bounds.
- Performed the requested adversarial rationale release-gate review without starting final rendering, highlighted citations, or email work. Re-read governing documentation, the complete rationale/model/validation/test implementation, and the full candidate diff.
- Found and fixed six material boundary issues: cross-document revision data was not rejected; unexpected provider exceptions could escape; extra schema fields and unbounded strings/arrays were accepted; confidence was model-supplied rather than deterministic; causal wording anywhere in a block could legitimize an unrelated nearby fact; and dense revision evidence could crowd out a direct results-preview block.
- Added exact schema/type/count validation, machine-readable warning filtering, sanitized catch-all provider failure handling, document-ID alignment, deterministic confidence, sentence-scoped explicit/hedged causal validation, valuation-versus-earnings separation, management-interaction/name/role gates, and conservative jargon validation.
- Tightened rating-change context patterns so generic upgrade/downgrade mentions do not classify the report. Direct context categories now receive first-occurrence selection priority; the previously missed real DNB results-preview signal is retained within the unchanged 24-block/12,000-character cap.
- Added adversarial synthetic cases for dense context, cross-document revisions, unexpected provider errors, extra schema fields, non-loopback endpoints, proximity-only causal output, valuation-as-earnings output, incorrect management roles, and incorrect jargon definitions.
- Ran the final retrieval-only evaluation over 15 reports across 13 broker/layout families. The 11 locator-backed cases selected the expected PDF 11/11. Across all cases, 344 candidate blocks had zero invalid IDs, page mismatches, text mismatches, or duplicates. Passage inputs ranged from 3,337 to 11,998 characters; estimated total prompt/payload size ranged from 7,157 to 17,287 characters.
- No local model configuration was available. Real-report semantic claims, rationale clarity, and context accuracy were therefore not fabricated or attributed to automated signal tests. The remaining semantic release gate requires a configured loopback model and manual review of every generated claim.
- Final adversarial-review verification passed 60/60 unit tests, Python compilation, indentation checks, and dependency consistency. Ruff and mypy are not configured in this repository. The 174-file source-PDF manifest hash was identical before and after the review; no PDF is tracked.
- Read the complete governing instructions, README, requirements, discovery, decisions, evaluation, development history, retrieval/evidence/revision/rationale implementations, and all existing tests before starting the precise-citation milestone. The committed baseline passed 60/60 tests using the existing local virtual environment.
- Chose a loopback-only standard-library viewer with in-memory PyMuPDF page rendering and an inline SVG overlay. No PDF.js download, external asset, new dependency, annotated derivative PDF, external service, or source-write path was introduced.
- Added a structured citation request/build/record model, ignored JSON store, revision and validated-rationale adapters, stable claim-specific IDs, corpus-relative source records, source size/digest checks, one-based page targets, word-to-line box merging, multi-page splitting, and explicit failed-resolution warnings. Models never receive or create citation IDs, paths, pages, URLs, or geometry.
- Added `find-rpt citations build`, `validate`, and `serve`. The builder accepts the existing deterministic locator or one direct PDF beneath the configured corpus. The server accepts loopback hosts only, routes opaque IDs only, suppresses request logs, sends no-store and restrictive browser headers, rejects symlinks/traversal/unindexed files, and revalidates the source before every response.
- The first real browser pass exposed a load-timing bug: a script attempted to scroll before the rendered page had stable dimensions. Replaced it with a normal-flow `#evidence-target` anchor in a page container with a fixed PDF aspect ratio, so the citation URL itself lands reliably without script timing.
- The first Nordea table citation highlighted the whole estimate-change block. Added deterministic metric/period line selection. Subsequent Intermonte review showed that the landing anchor should target the evidence row, not the first header box, and that same-row cells split across text blocks must be included. The selector now retains the requested period header, metric label, and horizontally aligned requested-period values.
- A metadata audit then caught two opposing edge cases: a compact BofA row had lost separately positioned old/new cells, while a Kepler header risked absorbing an unrelated same-height column. The final generic rule adds aligned cells only when the metric label contains no value and, without a separate period header, accepts cells only to the right of the metric label. Period-table tests ensure neighbouring years are excluded.
- Added 15 synthetic non-proprietary citation tests covering stable IDs, evidence/page resolution, finite and bounded boxes, invalid and wrong-document IDs, multi-line and multi-block evidence, multi-page splitting, stale sources, outside-corpus and unindexed paths, invalid routes and traversal, loopback binding, no-store/CSP headers, original-PDF routing, correct page targeting, CLI build/validate, structured revision input, period-specific table narrowing, and cache privacy.
- Built 176 real citations across BofA, Nordea, Intermonte, Stifel, and Kepler and validated 176/176 against current source fingerprints and geometry. Manually opened and rechecked ten final citations across those five layouts: correct page 10/10, correct relevant highlight 10/10, unrelated-text inclusion 0/10. Only hashed document IDs, broker, page, box count, and pass/fail outcomes are recorded.
- Stale detection was exercised on disposable synthetic PDFs at build, validator, and HTTP layers; the HTTP viewer returned conflict. Malicious/invalid route and unindexed-file tests returned not found without path disclosure. Real corpus PDFs were never modified for stale testing.
- Remaining citation limitations are raster rather than native-text viewing, no OCR, required local cache/server availability, and conservative retention of another fiscal period when it shares one compact source line. The final brief renderer and analyst-email escalation remain unimplemented as required.
- Final release-gate verification passed 75/75 tests, Python compilation, and `pip check`; Ruff and mypy remain unconfigured. All 176 real cached citations resolved with zero failed build requests. Invalid/traversal/unindexed routes, loopback binding rejection, correct page images, no-store headers, stale HTTP conflict, and source-PDF serving were exercised. The 173-file corpus manifest was `c925727c8aa16637ac314b1740a3dc56a3a55587c9626ca20b740ddecc723363` both before and after verification; Git tracks zero PDFs and zero citation/cache artifacts. All manually started viewer processes were stopped. No commit was created.
- Performed the requested final citation review without adding milestone scope. Re-read the governing documents, citation/evidence/CLI/server code, citation tests, and complete candidate diff; reran a fresh five-layout corpus build and visual sample.
- Fixed two material trust-boundary findings: citation base URLs could point away from loopback, and the original-PDF route validated a citation but then trusted duplicated document-index filename metadata. Loopback-origin validation, validated-citation source serving, cache-key identity checks, and non-empty finite page-geometry checks now fail closed. Added two synthetic regression tests.
- Final review verification passed 77/77 tests, the 17 citation/security tests, focused stale/multi-page/multi-block checks, Python compilation, and `pip check`; Ruff and mypy are not configured or installed. The fresh corpus build produced and resolved 176/176 citations with zero failed requests and identical repeated IDs; 165 use multiple blocks. Ten citations across five broker layouts passed visual report/page/passage/link checks with zero unrelated neighbouring passages. Four live invalid/traversal/unindexed/arbitrary-path requests returned 404 without path disclosure, and remote base URLs plus non-loopback binds were rejected. The 174-source-PDF manifest (corpus plus candidate brief) was unchanged before and after review. No commit was created.
